const HeroSection = ({ title }) => {
  return (
    <section className="hero">
      <div className="hero-content">
        <h1>{title}</h1>
      </div>
    </section>
  );
};

export default HeroSection;
